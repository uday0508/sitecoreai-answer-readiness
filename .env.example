# V1 does not require application secrets.
# Marketplace authentication is handled by the Marketplace SDK/host.

# Future external AI provider configuration should be server-only.
# Example:
# EXTERNAL_AI_API_KEY=
